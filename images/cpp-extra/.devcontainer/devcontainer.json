{
    "name": "Pre-build C++ extra",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": "..",
        "args": {
            "VARIANT": "${localEnv:DEVCONTAINER_BUILD_VARIANT:debian13}"
        }
    }
}
